"use client";

import CheckMarksComponent from "@/components/CheckMarksComponent";
import React from "react";

type Props = {};

const CheckMarksPage = (props: Props) => {
  return (
    <div>
      <CheckMarksComponent />
    </div>
  );
};

export default CheckMarksPage;
