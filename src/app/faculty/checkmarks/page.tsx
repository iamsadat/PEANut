"use client";

import CheckMarksComponent from "@/components/CheckMarksComponent";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <CheckMarksComponent />
    </div>
  );
};

export default page;
