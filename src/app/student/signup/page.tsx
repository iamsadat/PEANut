import React from "react";
import SignUp from "../../../components/signup/SignUp";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <SignUp />
    </div>
  );
};

export default page;
