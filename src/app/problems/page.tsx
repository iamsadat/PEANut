import AvailableProblems from "@/components/code/AvailableProblems";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AvailableProblems />
    </div>
  );
};

export default page;
