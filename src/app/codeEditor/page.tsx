import Landing from "@/components/code/Landing";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Landing />
    </div>
  );
};

export default page;
