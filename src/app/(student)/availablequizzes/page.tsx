"use client";

import QuizCard from "@/components/quiz/QuizCard";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <QuizCard />
    </div>
  );
};

export default page;
